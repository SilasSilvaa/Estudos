#include <iostream>

using namespace std;

int main()
{
    int x = 0;
    int m = 0;
    int a = 0;
    int b = 0;
    int c = 0;

    cout << (m * x) + c << endl;
    cout << 3 * (x / 2) << endl;
    cout << (3 * b * c) + 4 << endl;
    cout << ((3 * x) + 2) / (4 * a) - 1 << endl;
    cout << (a * pow(x, 2)) + (b * x) + c << endl;
    cout << pow(b, 2) - 4 * a * c << endl;
    cout << sqrt(10) << endl;

    return 0;
}